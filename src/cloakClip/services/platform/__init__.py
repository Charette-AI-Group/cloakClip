"""Platform-specific backends.

Each module here implements the same small interface for one operating
system. Adding support for another platform means adding a module and a
branch in the chooser — nothing above this layer changes.
"""
